import React from 'react'
import './style.css'
const Wallet = ({walletId}) => {
    console.log("🚀 ~ Wal ~ walletId:", walletId)
    return (
        <div>Wallet</div>
    )
}

export default Wallet